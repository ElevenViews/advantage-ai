import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Create this file for custom styles (optional)
import App from './App.js'; // Create an App.js file for your main component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
